<h1>id: <%= @taskapp.id %>のタスク編集ページ</h1>

<%= form_for(@taskapp) do |f| %>
  <%= f.label :content, "タスク"%>
  <%= f.text_field :content%>
  
  <%= f.submit "投稿"%>
<%end%>

<%= link_to "一覧に戻る", taskapps_path%>